<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Event Management - Event Details</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="static/css/main.css">
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
        <a class="navbar-brand" href="index.html">Sports Events</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDetails" aria-controls="navbarNavDetails" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDetails">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for(main.home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('event.create') }}">Create Event</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('main.booking-history') }}">Booking History</a>
                </li>
            </ul>
        </div>
    </nav>

    <h1>Events Details</h1>

    {% if events %}
        {% for event in events %}
            <div class="event">
                <h2>{{ event.name }}</h2>
                <p><strong>Description:</strong> {{ event.description }}</p>
                
                
                <img class="align-self-start mr-3" src="{{ event.image }}"> 
                
                
                <p><strong>Date:</strong> {{ event.date.strftime('%Y-%m-%d') }}</p>
                <p><strong>Time:</strong> {{ event.time.strftime('%H:%M') }}</p>
                <p><strong>Venue:</strong> {{ event.venue }}</p>
                <p><strong>States:</strong> {{ event.states}}</p>
            </div>
        {% endfor %}
    {% else %}
        <p>No events available at the moment.</p>
    {% endif %}

    {% if current_user.id == event.owner_id %}
        <a href="{{ url_for('event.edit_status', event_id=event.id) }}" class="btn btn-primary">Edit Status</a>
    {% endif %}

    <!-- Comments -->  
    <div class="row">  
        <div class="col-md-12">  
          <h2>Comments</h1>  
        </div>  
      </div>  
   
      <div class="row border-bottom"> 
        {{ render_form(form,"/events/{0}/comment".format(event.id)) }}
        
        {% for comment in event.comments %}
        <div class="col-md-12">  
          <b>{{ comment.user.name }} </b>
          <small>{{ comment.timestamp.strftime('%Y-%m-%d %H:%M') }}</small>
          <p>{{ comment.text }}</p>  
        </div> 
        {% endfor %} 
      </div>  
    

    <!-- Footer -->
    <footer class="bg-success text-light py-4 text-center">
        <p>© 2024 Event Management. All rights reserved.</p>
    </footer>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>